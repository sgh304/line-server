{
  "name": "line-server",
  "version": "1.0.0",
  "description": "A server that serves lines from a text file.",
  "main": "src/app.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Sam Holloway",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.4",
    "split": "^1.0.1"
  }
}
